package lando.systems.ld52.ui;

import com.kotcrab.vis.ui.widget.VisTable;

public class RightSideUI extends VisTable {
    public RightSideUI(float x, float y, float width, float height) {
        setSize(width, height);
        setPosition(x, y);
    }
}
